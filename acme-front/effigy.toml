[catalog]
alias = "acme-front"

[package_manager]
js = "bun"

[tasks]
health = [{ task = "check" }]
validate = [
  { task = "check" },
  { task = "build" }
]
qa = [{ task = "validate" }]

[tasks.dev]
run = "PUBLIC_API_URL=https://api.acme.test PUBLIC_FRONT_URL=https://acme.test PUBLIC_ADMIN_URL=https://admin.acme.test bun x vite dev --host 0.0.0.0 --port 41003 --strictPort"

[tasks.build]
run = "bun x vite build"

[tasks.preview]
run = "bun x vite preview"

[tasks.sync]
run = "bun x svelte-kit sync"

[tasks.prepare]
run = "bun x svelte-kit sync || echo ''"

[tasks.check]
run = [
  { task = "sync" },
  "bun x svelte-check --tsconfig ./tsconfig.json"
]

[tasks."check:watch"]
run = [
  { task = "sync" },
  "bun x svelte-check --tsconfig ./tsconfig.json --watch"
]
