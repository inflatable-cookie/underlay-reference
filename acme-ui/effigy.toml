[catalog]
alias = "acme-ui"

[package_manager]
js = "bun"

[tasks]
health = [{ task = "check" }]
validate = [{ task = "check" }]
qa = [{ task = "validate" }]
check = "bun x svelte-check --tsconfig ./tsconfig.json"

"refresh:deps" = "bun install"
