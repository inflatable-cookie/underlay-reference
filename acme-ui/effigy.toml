[catalog]
alias = "acme-ui"

[package_manager]
js = "bun"

[tasks]
health = [{ task = "check" }]
validate = [{ task = "check" }]
qa = [{ task = "validate" }]

[tasks.check]
run = "svelte-check --tsconfig ./tsconfig.json"

[tasks."refresh:deps"]
run = "bun install"
