[catalog]
alias = "acme-client"

[package_manager]
js = "bun"

[tasks]
health = [{ task = "check" }]
validate = [
  { task = "check" },
  { task = "build" }
]
qa = [{ task = "validate" }]

[tasks.build]
run = "tsc -p tsconfig.json"

[tasks.check]
run = "tsc -p tsconfig.json --noEmit"
