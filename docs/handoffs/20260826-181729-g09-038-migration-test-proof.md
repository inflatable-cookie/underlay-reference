---
title: g09.038 migration and test proof worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-08-26
updated: 2026-08-26
handoff_path: /Users/tom/Dev/projects/underlay-reference/docs/handoffs/20260826-181729-g09-038-migration-test-proof.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g09, migration, testing, reference]
---

## What This Thread Was Doing

The orchestrator promoted Underlay roadmap `g09.038` after the shared migration
and testing contracts landed. It then checked Underlay Reference's planning
front doors, repaired a contradiction between the generation index and four
completed `g01` roadmaps, and mapped the implementation surface for this proof.

This is one bounded worker lane. It cuts Underlay Reference over to the baseline
root-state and package-owned migration interface, proves that interface against
the positively identified disposable local database, restores the API health
minimum, and converts one existing direct-Axum route-test slice to
`underlay_testing::TestServer`.

The target repo also has an independent retained-surface lane at `g01.007`.
That lane is legitimate, still active, and not part of this handoff.

## Why It Matters

Underlay Reference is the first consumer proof for the new migration and test
contracts. The remaining five consumer roadmaps must not copy an unproved task
shape. A clean reference implementation gives those later lanes a known state
ordering, fail-closed replay proof, cheap API health baseline, and bounded
shared-server example.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `git@github.com:inflatable-cookie/underlay-reference.git`
- **Planning branch:** `main`
- **Planning base commit:** `2fc07785f73d5e16e327e11437b58069c4930cc4`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `2fc07785f73d5e16e327e11437b58069c4930cc4` after a fresh fetch on
  2026-08-26.
- **Planning checkout:** clean and aligned with `origin/main` when this handoff
  was written.
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Planning artifacts included at the base:** target planning-front-door
  repair at `2fc07785`; Underlay's external `g09.038` authority is at
  `5b533d02e23c3f3a5e2f7c9f4b372f35d7f8baea` in the sibling Underlay repo.
- **Worker branch:** `worker/g09-038-underlay-reference-migration-test-proof`
  is the suggested fallback name only. Accept the launcher's clean non-`main`
  branch when one is supplied.
- **Worker worktree:** launcher-provided clean registered worktree; record its
  actual path during preflight. No manual fallback path has been authorised or
  guessed.
- **Worktree creation command:** launcher-owned by default. If the current and
  named contexts are unusable, read `.agents.local.env`, require
  `AGENTS_WORKTREE_CONTAINER_DIR`, ask the operator if it is absent, then use
  `git worktree add -b <unique-branch> <AGENTS_WORKTREE_CONTAINER_DIR>/<unique-name> origin/main`.
- **Worker worktree policy:** first use the clean, dedicated, non-`main`
  registered worktree supplied by the launcher, even if its generated path or
  branch differs from these handoff placeholders. Record the actual path and
  branch and never create a second worktree for that reason. If the current
  context is unusable, use the named worktree when it matches; only then read
  `.agents.local.env`, require `AGENTS_WORKTREE_CONTAINER_DIR`, and create a
  unique manual worktree and branch under that container from `origin/main`.
  Ask the operator first if the file or key is absent; never use `/tmp`,
  `TMPDIR`, or a guessed path.
- **Active target spec lane:** `docs/specs/001-retained-underlay-surface-strict-lane.md`
  with `docs/roadmaps/g01/007-retained-underlay-surface-formalization.md` and
  `docs/specs/batch-cards/001-audit-retained-acme-admin-underlay-surface.md`.
  It is independent and must remain untouched.
- **Roadmap milestone:** external Underlay authority at
  `/Users/tom/Dev/projects/underlay/docs/roadmaps/g09/038-underlay-reference-migration-and-test-proof.md`.
- **Ready cards, in order:** none. `g09.038` is deliberately a single runnable
  roadmap, not a batch-card cycle.
- **Allowed runway:** the complete `g09.038` implementation and proof in one
  reviewable PR.
- **Remaining card budget:** one roadmap milestone; pause after its PR is ready.
- **Dispatch topology:** serial inside Underlay Reference. The other consumer
  roadmaps remain blocked until this reference proof merges.
- **Parallel safety check:** no other worker may mutate this repo's migration,
  state, API test, or workflow-doc surfaces during this lane. The independent
  target `g01.007` lane is planning state, not shared implementation scope.
- **Canonical refs:** target `AGENTS.md`, `docs/architecture/000-overview.md`,
  `docs/architecture/product-guardrails.md`, and
  `docs/policy/001-working-rules.md`; Underlay contracts
  `docs/contracts/021-database-migration-and-schema-workflow.md`,
  `docs/contracts/022-testing-posture-and-shared-harnesses.md`, and
  `docs/contracts/023-release-and-compatibility-rollout.md`; Underlay usage
  policy `docs/usage/migration/000-state-layout-and-effigy.md`.
- **Model capability profile:** frontier coding model with high reasoning. This
  lane includes persistence configuration and a destructive from-empty proof.
- **Tool/runtime restrictions:** use the repo-local Effigy skill and task
  surface first. Do not edit release workflows, change dependency sources or
  versions, touch another repo, or use any database other than the exact local
  target below.
- **Required validation:** `effigy tasks`; `effigy state plan`;
  `effigy state apply local --yes`; both introduced routed package
  `migration:*` apply and reset/replay selectors; `effigy acme-api/health`; one
  targeted API route test; `effigy health`; `effigy validate`; `effigy qa`;
  `effigy acme-docs/qa:docs`; `effigy acme-docs/qa:northstar`; and a search
  proving no active `db:migrate`, `db:reset`, or `db:drop` surface remains.
- **PR base/head:** `main` / worker branch.
- **PR URL:** pending.
- **Review state:** awaiting implementation and review.
- **Merge authorisation:** none. The worker must not merge.

The approved destructive proof boundary is exact:

- Effigy system: `underlay-reference-dev`
- PostgreSQL container: `underlay-reference-dev-postgres-1`
- database: `acme`
- user: `postgres`
- host binding: `127.0.0.1:19932`
- persistent volume: `underlay-reference-dev-postgres-data`
- shared services reported by Effigy: none
- readiness at promotion: accepting connections

Re-prove every one of these facts immediately before the first mutation. Stop
if any command resolves to another host, project, database, container, volume,
or shared service.

## Boundaries

Please keep this run inside the named runway:

- **In scope:** root `effigy.toml`; `apps/acme-api/effigy.toml`; the existing
  database migration/reset entry points under `apps/acme-api/crates/db/` only
  as needed to expose fail-closed package tasks; one bounded route-test module
  in `apps/acme-api/crates/api/tests/api_tests.rs`; the minimum Cargo dependency
  wiring for `underlay-testing`; active root/API workflow docs and agent notes;
  and one execution log under `docs/logs/`.
- **Out of scope:** implementation work for the target `g01.007` retained
  admin-surface lane; changes under `apps/acme-admin`, `apps/acme-front`, or
  unrelated `packages/*`; the other five consumer repos; Underlay code or
  planning edits; releases; dependency re-pointing; GitHub workflows; shared
  harness redesign; broad route-test conversion; Doctor cleanup; and
  compatibility aliases for retired `db:*` selectors.
- Preserve separate structural migrations in `apps/acme-api/migrations/` and
  dev overlays in `apps/acme-api/migrations_dev/`. Both state application and
  reset/replay must fail when the dev overlay fails.
- Keep application state, auth providers, and fixtures app-owned. Adopt
  `TestServer` only where an existing state-free direct-Axum test already fits.
- Do not print secrets or database contents into durable logs. Record commands,
  target identity, outcome, and non-sensitive replay evidence.
- Do not invent unsupported Effigy state keys. Check the installed Effigy
  surface and the canonical state policy before writing the root stack.
- Do not invent architecture, change contracts, widen the roadmap, or choose an
  unresolved product, API, persistence, or security decision.
- This handoff represents one worker lane. Do not edit another lane's assigned
  scope; if shared mutable scope or a hidden dependency appears, stop and
  report it through the operator.
- Work only in the selected clean worker worktree: prefer the current
  launcher-provided worktree and record its actual path and branch. Never edit
  the orchestrator's planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge remains a separate operator-authorised action.

## Important Context

- **Planning lineage:** Underlay `g09.035` assessed the fleet migration
  contract, `g09.036` assessed test posture, `g09.037` repaired and documented
  the shared surface, and `g09.038` is the first consumer proof. The target
  planning front doors were reconciled at `2fc07785`; do not reopen completed
  target roadmaps `g01.008` through `g01.011`.
- **Why the roadmap is ready:** the shared contracts are active, Underlay
  `g09.037` is complete, target `main` is current, and the disposable local
  PostgreSQL target was positively identified without shared services.
- **Current migration surface:** `apps/acme-api/effigy.toml` exposes
  `db:drop`, `db:migrate`, and `db:reset`; the underlying binaries are
  `crates/db/src/bin/reset_dev_db.rs` and `migrate_dev_db.rs`; structural and
  dev migration ownership is in `crates/db/src/lib.rs`.
- **Required task cutover:** root owns the local state stack behind
  `effigy state plan` and `effigy state apply local --yes`; the API package owns
  the concrete `migration:*` apply and reset/replay tasks. Remove `db:*`
  selectors and active references without aliases.
- **Current test surface:** all direct `Router::oneshot` examples live in
  `apps/acme-api/crates/api/tests/api_tests.rs`. The state-free
  `health_tests` module is the smallest `TestServer` candidate. Do not expand
  this into the app-state or auth-heavy modules.
- **Shared harness API:** Underlay `v0.9.4` exposes
  `underlay_testing::TestServer` behind its `server` feature. Add only the
  released dependency wiring required by the selected API test; retain the
  repo's existing `v0.9.4` Git-tag posture.
- **Current health drift:** `apps/acme-api/effigy.toml` routes `health` only to
  `fmt`, while `apps/acme-api/AGENTS.md` claims build is already part of the
  stable health baseline. Make the task and active guidance agree by adding a
  cheap Cargo build/check baseline.
- **Active docs drift:** root `README.md`, `apps/acme-api/README.md`, and
  `docs/architecture/000-overview.md` advertise retired `db:*` commands. The
  root README also claims PostgreSQL data is stored under
  `.effigy/runtime/data/postgres`, while the positively identified live system
  uses the repo-scoped named volume
  `underlay-reference-dev-postgres-data`. Correct the active runtime wording in
  this workflow-doc batch; do not rewrite historical logs.
- **Known unrelated tool debt:** existing Doctor findings include
  `test.exclude_catalogs` support/reference noise and broad scan findings. They
  are not acceptance failures for this roadmap unless the implementation
  creates a new one. Do not turn this lane into Doctor cleanup.
- **Decisions and preferences:** this is a breaking local task-interface
  cutover with no compatibility window. The user required roadmap-led
  Northstar execution; do not create or cycle batch cards for this lane.
- **Open tensions:** Effigy's installed state schema is the authority for the
  exact TOML shape. If the baseline contract cannot express ordered structural
  plus dev-overlay package tasks without broad app changes, stop and return the
  conflict instead of improvising a second migration system.
- **Report after:** (1) task/state cutover plus a resolved state plan; (2) exact
  local-target from-empty and replay proof; (3) health/TestServer/docs gates and
  a ready PR.
- **Report to:** the operator, who will relay progress to the orchestrator.

## Suggested Next Move

Read this handoff from the top, then run the quick worktree-safety preflight in
`## Completion Protocol` before broad repository reads. Accept a clean,
registered, non-`main` launcher worktree as-is and record its actual path and
branch.

From that worktree, read root and API `AGENTS.md`, the external `g09.038`
roadmap, the three Underlay contracts, the state usage policy, and the target
architecture and working rules. Run `effigy tasks`, `effigy test --plan`, and
the installed Effigy state/config discovery surface before editing.

Take the first coherent chunk as one task-interface change: declare the root
local state stack, expose package-owned `migration:*` apply and reset/replay
tasks, remove the `db:*` selectors, and make API `health` include a cheap Cargo
baseline. Run `effigy state plan` before any destructive proof. Immediately
before `state apply` or reset, re-check the exact Effigy system, container,
database, host binding, volume, shared-service state, and readiness listed
above. If they match, run the from-empty apply and replay proof. If they do not,
stop.

Then convert only the state-free health route tests to `TestServer`, update the
active workflow docs and agent note, write one execution log, run the full gate,
push the worker branch, and open the PR. Do not update or close Underlay's
external roadmap from this worker repo; the orchestrator owns that after merge.

## Completion Protocol

### Before you start

1. Read this handoff path. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Then run
   one quick read-only safety probe before broad repository reads:
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root and branch and do not compare them with the suggested fallback
   path or branch or create another worktree merely because they differ.
3. Only if the current context is `main`, dirty, unregistered, or otherwise
   unusable should you inspect the named worktree. If that also cannot be used,
   read `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`; if it
   is absent, ask the operator before creating the file or worktree. Then
   create a unique worktree and branch under that container from pushed
   `origin/main`, record the actual path and branch, and run all subsequent
   commands there. Never use `/tmp`, `TMPDIR`, or a guessed path; never clean,
   reset, stash over, or discard the original checkout's dirty state. If the
   launcher supplied a dirty or `main` worktree, stop and report it instead of
   silently creating a second worktree.
4. From the selected worktree, confirm `git rev-parse HEAD` equals
   `git rev-parse origin/main`, confirm
   `git merge-base --is-ancestor 2fc07785f73d5e16e327e11437b58069c4930cc4 HEAD`
   succeeds, and confirm this handoff file exists in the selected `HEAD`.
5. Read the external roadmap, target `AGENTS.md`, API `AGENTS.md`, target
   architecture/working rules, and canonical Underlay contracts and state
   usage policy named above.
6. Run `effigy tasks`, `effigy test --plan`, and the installed Effigy state or
   config help needed to verify the manifest shape. Record what you ran.

### While you work

- Complete the roadmap in the three meaningful chunks listed above. Keep
  commits aligned with those chunks, not arbitrary model turns.
- After each chunk, report changed files, validation actually run, remaining
  work, new risks, and blockers through the operator.
- Stop before any database mutation unless every approved boundary fact has
  just been re-proved.
- Stop if the state stack cannot fail closed, if `TestServer` would require
  moving app state/auth/fixtures into Underlay, if a contract is missing, if
  scope expands, or if validation changes the plan.
- Do not quietly turn an open question into new architecture.

### When the assigned runway is complete

1. Run the required final validation:
   - `effigy tasks`
   - `effigy state plan`
   - `effigy state apply local --yes` against only the approved local target
   - every introduced routed `acme-api/migration:*` apply and reset/replay task
   - `effigy acme-api/health`
   - the targeted `health_tests` API route-test slice, using Effigy's resolved
     test surface or a targeted raw Cargo fallback only if Effigy cannot express
     it
   - `effigy health`
   - `effigy validate`
   - `effigy qa`
   - `effigy acme-docs/qa:docs`
   - `effigy acme-docs/qa:northstar`
   - `rg -n 'db:(migrate|reset|drop)' README.md effigy.toml apps docs --glob '!docs/logs/**' --glob '!docs/handoffs/**'`, which must find no active task, agent, guide, README, or runtime message
   - `git diff --check`
2. Add one target execution log under `docs/logs/2026-08/` with the actual
   worktree/branch, exact non-sensitive local target identity, from-empty and
   replay outcomes, changed task/test surfaces, validation, and any residual
   risk. Keep target `g01.007` planning state unchanged.
3. Push the selected worker branch.
4. Open a reviewable PR against current pushed `main`. This handoff's
   `2fc07785f73d5e16e327e11437b58069c4930cc4` is the planning base before the
   handoff commit, not a self-referential hash for the commit containing this
   file.
5. In the PR body, link this handoff, the external `g09.038` roadmap, changed
   state/task/test/docs surfaces, destructive-proof evidence, validation, and
   unresolved items. State clearly that target `g01.007` was preserved.
6. Report the PR URL and evidence to the operator. Do not merge.

### Review and merge path

The orchestrator will review the PR against the canonical refs, diff, and
checks. Current review state: awaiting implementation.

When the orchestrator and worker share a GitHub identity, formal self-approval
is unavailable, so the orchestrator posts the verdict as a PR comment; that
comment is the canonical review record. If changes are requested, make only
those changes on this branch, push again, and report back through the operator.
The PR should link the roadmap, handoff, changed surfaces, evidence, validation,
and unresolved items. The operator must explicitly authorise any merge.

- **Requested changes:** none yet.
- **Closeout refs:** target execution log; Underlay
  `docs/roadmaps/g09/038-underlay-reference-migration-and-test-proof.md`;
  Underlay `docs/roadmaps/README.md`; Underlay
  `docs/roadmaps/generation-index.md`; target `docs/roadmaps/README.md`; and
  target `docs/roadmaps/g01/README.md`.

### Handoff closeout

Before calling the runway complete, leave the target execution log and PR
evidence honest. Do not edit the independent target `g01.007` spec, roadmap, or
card. The next task after an authorised merge is for the orchestrator to close
Underlay `g09.038`, promote `g09.039` through `g09.043` as independent consumer
roadmaps, and dispatch those lanes. If the work is blocked, record the blocker
and stop rather than making the handoff look complete.
